---
title: "Hello, conflicts resolved"
format: html
---

```{r}
summary(cars)

library(ggplot2)

ggplot(cars, aes(x = speed, y = dist)) +
  geom_point() +
  labs(x = "Speed (mph)", y = "Stopping distance (ft)") +
  theme_minimal()
```
